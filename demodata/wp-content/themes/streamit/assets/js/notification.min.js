"use strict";
! function(i) {
    i(document).ready(function() {
        i(".streamit-notification").click(function(t) {
            0 === i(t.target).closest(".streamit_notification_wrapper").length && i(this).toggleClass("notification-open"), i(document).click(function(t) {
                0 === i(t.target).closest(".streamit-notification").length && i(".streamit-notification").removeClass("notification-open")
            })
        }), i.ajax({
            url: streamit_loadmore_params.ajaxurl,
            data: {
                action: "streamit_notification_data"
            },
            type: "POST",
            success: function(t) {
                i(".streamit-notification").append(t);
                t = i(".streamit_notification_wrapper").data("notification_count");
                void 0 !== t ? i(".streamit-notification .notification-count").html(t) : i(".streamit-notification .notification-count").remove()
            }
        })
    })
}(jQuery);