"use strict";

function _typeof(t) {
    return (_typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(t) {
        return typeof t
    } : function(t) {
        return t && "function" == typeof Symbol && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t
    })(t)
}

function continueWatchHandler(t) {
    var e, n = document.querySelector(".iq-main-slider video").player;
    n.setCurrentTime(null === (e = streamit_continue_watch) || void 0 === e ? void 0 : e.watchedTime.watchedTime), n.play(), jQuery(t).closest(".streamit-video-continue-watch-container").fadeOut()
}

function continueWatchRemoveHandler(t) {
    jQuery(t).closest(".streamit-video-continue-watch-container").fadeOut()
}

function watchHandler(t, e, n) {
    jQuery.ajax({
        url: streamit_loadmore_params.ajaxurl,
        async: !1,
        data: {
            action: "streamit_update_watched",
            postID: streamit_continue_watch.ID,
            watchedTime: t,
            watchedTotalTime: e,
            watchedTimePercentage: n,
            nonce: streamit_continue_watch.nonce
        },
        type: "GET"
    })
}

function watchedAjaxHandler() {
    jQuery.ajax({
        url: streamit_loadmore_params.ajaxurl,
        async: !1,
        data: {
            action: "streamit_delete_watched",
            postID: streamit_continue_watch.ID,
            nonce: streamit_continue_watch.nonce
        },
        type: "GET"
    })
}! function(e) {
    e(document).ready(function() {
        e(".remodal").wrap('<div class="streamit-model-wrapper"></div>')
    }), e(window).on("load", function() {
        var t;
        "undefined" != typeof streamit_continue_watch && "object" == _typeof(null === (t = streamit_continue_watch) || void 0 === t ? void 0 : t.watchedTime) && e(".streamit-video-continue-watch-container").closest(".streamit-video-continue-watch-container").fadeIn()
    })
}(jQuery);